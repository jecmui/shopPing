# ShopAlert
This project was created for UB Hacking 2025. 

## Project Setup
To set up the project, make sure you have [pnpm](https://pnpm.io/) installed and run `pnpm install`. To run the project, run `docker compose up -d --build`. 